services:
  web:
    image: hfts-web:dev
    container_name: hfts-web
    ports:
      - "10001:10001"
    depends_on:
      api:
        condition: service_started
    restart: unless-stopped

  api:
    image: hfts-api:dev
    container_name: hfts-api
    ports:
      - "10002:10002"
    environment:
      HFTS_ENV:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/high_frequency_tickets
      REDIS_URL: redis://redis:6379
      PUBSUB_EMULATOR_HOST: pubsub:8085
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      pubsub:
        condition: service_healthy
    restart: unless-stopped

  worker:
    image: hfts-worker:dev
    container_name: hfts-worker
    ports:
      - "10003:10003"
    environment:
      HFTS_ENV:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/high_frequency_tickets
      REDIS_URL: redis://redis:6379
      PUBSUB_EMULATOR_HOST: pubsub:8085
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      pubsub:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: hfts-redis
    ports:
      - "10004:6379"
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

  pubsub:
    image: gcr.io/google.com/cloudsdktool/cloud-sdk:emulators
    platform: linux/amd64
    container_name: hfts-pubsub
    ports:
      - "10005:8085"
    command: gcloud beta emulators pubsub start --host-port=0.0.0.0:8085
    environment:
      PUBSUB_PROJECT_ID: high-frequency-ticket-system
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8085"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: hfts-postgres
    ports:
      - "10006:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: high_frequency_tickets
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d high_frequency_tickets"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  prometheus:
    image: prom/prometheus:v3.4.0
    container_name: hfts-prometheus
    ports:
      - "10007:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --web.enable-remote-write-receiver
    restart: unless-stopped

  grafana:
    image: grafana/grafana:12.0.1
    container_name: hfts-grafana
    ports:
      - "10008:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    environment:
      GF_SECURITY_ADMIN_PASSWORD: admin
      # Serverseitiges Rendern von Panels als PNG (`pnpm spike:graphs`, ADR-030).
      # Die Callback-URL ist die Adresse, unter der der Renderer Grafana im
      # Compose-Netz erreicht — nicht localhost, das waere der Renderer selbst.
      GF_RENDERING_SERVER_URL: http://renderer:8081/render
      GF_RENDERING_CALLBACK_URL: http://grafana:3000/
    depends_on:
      - prometheus
    restart: unless-stopped

  redis_exporter:
    image: oliver006/redis_exporter:v1.62.0
    container_name: hfts-redis-exporter
    ports:
      - "10009:9121"
    environment:
      REDIS_ADDR: redis://redis:6379
    depends_on:
      - redis
    restart: unless-stopped

  renderer:
    image: grafana/grafana-image-renderer:3.12.9
    container_name: hfts-grafana-renderer
    ports:
      - "10010:8081"
    environment:
      # Ein Lasttest-Dashboard hat viele Serien; der Default-Timeout (30s) reicht
      # bei kaltem Chromium-Start nicht immer.
      RENDERING_TIMEOUT: "60"
      ENABLE_METRICS: "true"
    restart: unless-stopped

volumes:
  redis_data:
  postgres_data:
  prometheus_data:
  grafana_data:
