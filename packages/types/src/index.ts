/**
 * Shared Zod schemas and TypeScript types for the High-Frequency Ticket System.
 *
 * All DTOs (Request/Response types) are defined here as Zod schemas.
 * TypeScript types are inferred via z.infer<> — no manual type declarations.
 */
export {};
