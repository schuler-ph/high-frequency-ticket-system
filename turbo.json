{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalEnv": ["HTS_ENV_PROFILE", "DATABASE_URL", "NODE_ENV", "LOG_LEVEL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "../../config/env/*.env"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "lint": {
      "dependsOn": []
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "test": {
      "dependsOn": [],
      "inputs": ["$TURBO_DEFAULT$", "../../config/env/*.env"],
      "outputs": []
    },
    "test:ci": {
      "dependsOn": [],
      "inputs": ["$TURBO_DEFAULT$", "../../config/env/*.env"],
      "outputs": ["coverage/**"]
    },
    "@repo/e2e-tests#test:ci": {
      "dependsOn": [],
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    }
  }
}
