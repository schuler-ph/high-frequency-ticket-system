{
  "name": "api",
  "version": "1.0.0",
  "description": "This project was bootstrapped with Fastify-CLI.",
  "type": "module",
  "main": "app.ts",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "pnpm run build && tsc -p test/tsconfig.json && c8 node --loader ts-node/esm --test \"test/**/*.ts\"",
    "start": "pnpm run build && fastify start -l info -p 10001 dist/app.js",
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "tsc-watch --onSuccess \"pnpm run dev:start\"",
    "dev:start": "fastify start -P -l info -p 10001 dist/app.js",
    "check-types": "tsc --noEmit",
    "lint": "eslint . --max-warnings 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^6.0.0",
    "@fastify/cors": "^11.2.0",
    "@fastify/redis": "^7.2.0",
    "@fastify/sensible": "^6.0.0",
    "@google-cloud/pubsub": "^5.3.0",
    "@repo/env": "workspace:*",
    "@repo/types": "workspace:*",
    "fastify": "^5.0.0",
    "fastify-cli": "^7.4.1",
    "fastify-plugin": "^5.0.0",
    "fastify-type-provider-zod": "^6.1.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^24.0.8",
    "c8": "^10.1.2",
    "concurrently": "^9.0.0",
    "eslint": "^9.39.1",
    "tsc-watch": "^6.3.1",
    "ts-node": "^10.4.0",
    "typescript": "~5.9.2"
  }
}
