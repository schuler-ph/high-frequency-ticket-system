apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      terminationGracePeriodSeconds: 35
      containers:
        - name: worker-container
          image: hfts-worker:dev
          ports:
            - containerPort: 10003
          envFrom:
            - configMapRef:
                name: worker-config
            - secretRef:
                name: worker-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 10003
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"